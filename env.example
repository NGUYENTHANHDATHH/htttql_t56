# Environment Configuration
# Copy this file to .env and update the values

# Server URL for socket connection (Render backend URL)
VITE_SERVER_URL=https://your-backend-app-name.onrender.com

# API Keys (if needed)
GEMINI_API_KEY=your_gemini_api_key_here

# For Vercel deployment, set these in Vercel dashboard:
# VITE_SERVER_URL = https://your-render-backend-url.onrender.com

# For Render backend deployment, set these in Render dashboard:
# NODE_ENV = production
# PORT = (Render sets this automatically)
